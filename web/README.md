# Web frontend

React + TypeScript frontend using Vite, Tailwind CSS, React Router, and Axios.

## Run

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:5000`.

## API URL

Set `VITE_API_URL` (see `.env.example`).
